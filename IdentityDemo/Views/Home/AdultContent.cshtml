@{
    ViewData["Title"] = "Adult Content";
}

<div class="alert alert-warning">
    <h4>🔞 Age Restricted Content</h4>
    <p>This page requires users to be 18 or older based on their age claim.</p>
    <p>Your age claim: @User.FindFirst("age")?.Value</p>
</div>

<div class="card">
    <div class="card-header">
        <h5>Claims-Based Authorization Demo</h5>
    </div>
    <div class="card-body">
        <p>This demonstrates how claims can be used for fine-grained authorization beyond just roles.</p>
        <p>The authorization policy "MinimumAge18" checks for specific age claim values.</p>
        
        <h6>Your Current Claims:</h6>
        <ul>
            @foreach (var claim in User.Claims)
            {
                <li><strong>@claim.Type:</strong> @claim.Value</li>
            }
        </ul>
    </div>
</div>
